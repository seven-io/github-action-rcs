import { AbstractResource } from '../AbstractResource';
import { SmsDeleteParams, SmsDeleteResponse, SmsParams, SmsResponse } from './types';
export default class SmsResource extends AbstractResource {
    delete: (p: SmsDeleteParams) => Promise<SmsDeleteResponse>;
    dispatch: (p: SmsParams) => Promise<SmsResponse>;
}
